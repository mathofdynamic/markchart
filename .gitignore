node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local Cloudflare config & state (contains project-specific IDs) — use the
# committed *.example templates instead.
wrangler.toml
.dev.vars
.wrangler/

# Never commit OAuth client secret files
client_secret*.json

# Behdad audit workspace (approval markers / suppressions) — local only
.behdad/

# Windows folder-icon cruft
desktop.ini
folderico-*.ico
